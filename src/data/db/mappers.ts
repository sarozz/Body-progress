import type { BodyCheckin, Profile, Sex } from '@/types';
import type { UnitSystem } from '@/core/units';

import type { CheckinRow, ProfileRow } from './schema';

export const profileFromRow = (r: ProfileRow): Profile => ({
  id: r.id,
  displayName: r.displayName,
  sex: (r.sex as Sex | null) ?? null,
  birthDate: r.birthDate,
  heightCm: r.heightCm,
  unitSystem: (r.unitSystem as UnitSystem) ?? 'metric',
  goalType: (r.goalType as Profile['goalType']) ?? null,
  goalTargetWeightKg: r.goalTargetWeightKg,
  goalTargetWaistCm: r.goalTargetWaistCm,
  goalTargetBodyFat: r.goalTargetBodyFat,
  goalDeadline: r.goalDeadline,
  updatedAt: r.updatedAt,
});

export const profileToRow = (
  p: Profile,
  syncState: 'pending' | 'synced' = 'pending',
): ProfileRow => ({
  id: p.id,
  displayName: p.displayName,
  sex: p.sex,
  birthDate: p.birthDate,
  heightCm: p.heightCm,
  unitSystem: p.unitSystem,
  goalType: p.goalType,
  goalTargetWeightKg: p.goalTargetWeightKg,
  goalTargetWaistCm: p.goalTargetWaistCm,
  goalTargetBodyFat: p.goalTargetBodyFat,
  goalDeadline: p.goalDeadline,
  updatedAt: p.updatedAt,
  syncState,
});

export const checkinFromRow = (r: CheckinRow): BodyCheckin => ({
  id: r.id,
  userId: r.userId,
  checkinDate: r.checkinDate,
  weightKg: r.weightKg,
  bodyFatPct: r.bodyFatPct,
  chestCm: r.chestCm,
  waistCm: r.waistCm,
  hipsCm: r.hipsCm,
  neckCm: r.neckCm,
  shouldersCm: r.shouldersCm,
  bicepLeftCm: r.bicepLeftCm,
  bicepRightCm: r.bicepRightCm,
  tricepLeftCm: r.tricepLeftCm,
  tricepRightCm: r.tricepRightCm,
  forearmLeftCm: r.forearmLeftCm,
  forearmRightCm: r.forearmRightCm,
  thighLeftCm: r.thighLeftCm,
  thighRightCm: r.thighRightCm,
  calfLeftCm: r.calfLeftCm,
  calfRightCm: r.calfRightCm,
  notes: r.notes,
  photoPath: r.photoPath,
  createdAt: r.createdAt,
  updatedAt: r.updatedAt,
});

export const checkinToRow = (
  c: BodyCheckin,
  syncState: 'pending' | 'synced' = 'pending',
): CheckinRow => ({
  id: c.id,
  userId: c.userId,
  checkinDate: c.checkinDate,
  weightKg: c.weightKg,
  bodyFatPct: c.bodyFatPct,
  chestCm: c.chestCm,
  waistCm: c.waistCm,
  hipsCm: c.hipsCm,
  neckCm: c.neckCm,
  shouldersCm: c.shouldersCm,
  bicepLeftCm: c.bicepLeftCm,
  bicepRightCm: c.bicepRightCm,
  tricepLeftCm: c.tricepLeftCm,
  tricepRightCm: c.tricepRightCm,
  forearmLeftCm: c.forearmLeftCm,
  forearmRightCm: c.forearmRightCm,
  thighLeftCm: c.thighLeftCm,
  thighRightCm: c.thighRightCm,
  calfLeftCm: c.calfLeftCm,
  calfRightCm: c.calfRightCm,
  notes: c.notes,
  photoPath: c.photoPath,
  createdAt: c.createdAt,
  updatedAt: c.updatedAt,
  syncState,
});

export const checkinToSupabase = (c: BodyCheckin): Record<string, unknown> => ({
  id: c.id,
  user_id: c.userId,
  checkin_date: c.checkinDate,
  weight_kg: c.weightKg,
  body_fat_pct: c.bodyFatPct,
  chest_cm: c.chestCm,
  waist_cm: c.waistCm,
  hips_cm: c.hipsCm,
  neck_cm: c.neckCm,
  shoulders_cm: c.shouldersCm,
  bicep_left_cm: c.bicepLeftCm,
  bicep_right_cm: c.bicepRightCm,
  tricep_left_cm: c.tricepLeftCm,
  tricep_right_cm: c.tricepRightCm,
  forearm_left_cm: c.forearmLeftCm,
  forearm_right_cm: c.forearmRightCm,
  thigh_left_cm: c.thighLeftCm,
  thigh_right_cm: c.thighRightCm,
  calf_left_cm: c.calfLeftCm,
  calf_right_cm: c.calfRightCm,
  notes: c.notes,
  photo_path: c.photoPath,
  updated_at: c.updatedAt,
});

export const checkinFromSupabase = (
  r: Record<string, unknown>,
): BodyCheckin => ({
  id: String(r.id),
  userId: String(r.user_id),
  checkinDate: String(r.checkin_date),
  weightKg: (r.weight_kg as number) ?? null,
  bodyFatPct: (r.body_fat_pct as number) ?? null,
  chestCm: (r.chest_cm as number) ?? null,
  waistCm: (r.waist_cm as number) ?? null,
  hipsCm: (r.hips_cm as number) ?? null,
  neckCm: (r.neck_cm as number) ?? null,
  shouldersCm: (r.shoulders_cm as number) ?? null,
  bicepLeftCm: (r.bicep_left_cm as number) ?? null,
  bicepRightCm: (r.bicep_right_cm as number) ?? null,
  tricepLeftCm: (r.tricep_left_cm as number) ?? null,
  tricepRightCm: (r.tricep_right_cm as number) ?? null,
  forearmLeftCm: (r.forearm_left_cm as number) ?? null,
  forearmRightCm: (r.forearm_right_cm as number) ?? null,
  thighLeftCm: (r.thigh_left_cm as number) ?? null,
  thighRightCm: (r.thigh_right_cm as number) ?? null,
  calfLeftCm: (r.calf_left_cm as number) ?? null,
  calfRightCm: (r.calf_right_cm as number) ?? null,
  notes: (r.notes as string) ?? null,
  photoPath: (r.photo_path as string) ?? null,
  createdAt: String(r.created_at ?? new Date().toISOString()),
  updatedAt: String(r.updated_at ?? new Date().toISOString()),
});

export const profileToSupabase = (p: Profile): Record<string, unknown> => ({
  id: p.id,
  display_name: p.displayName,
  sex: p.sex,
  birth_date: p.birthDate,
  height_cm: p.heightCm,
  unit_system: p.unitSystem,
  goal_type: p.goalType,
  goal_target_weight_kg: p.goalTargetWeightKg,
  goal_target_waist_cm: p.goalTargetWaistCm,
  goal_target_body_fat: p.goalTargetBodyFat,
  goal_deadline: p.goalDeadline,
});

export const profileFromSupabase = (r: Record<string, unknown>): Profile => ({
  id: String(r.id),
  displayName: (r.display_name as string) ?? null,
  sex: (r.sex as Sex) ?? null,
  birthDate: (r.birth_date as string) ?? null,
  heightCm: (r.height_cm as number) ?? null,
  unitSystem: ((r.unit_system as UnitSystem) ?? 'metric'),
  goalType: (r.goal_type as Profile['goalType']) ?? null,
  goalTargetWeightKg: (r.goal_target_weight_kg as number) ?? null,
  goalTargetWaistCm: (r.goal_target_waist_cm as number) ?? null,
  goalTargetBodyFat: (r.goal_target_body_fat as number) ?? null,
  goalDeadline: (r.goal_deadline as string) ?? null,
  updatedAt: String(r.updated_at ?? new Date().toISOString()),
});
